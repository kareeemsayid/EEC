# Local development: copy to backend/.env
# In production these are set in the Azure Function App Configuration tab.
SQL_SERVER=eec-sql-serverr.database.windows.net
SQL_DATABASE=EEC_DB
SQL_USER=eecadmin
SQL_PASSWORD=your_password_here
# Optional: used only by backend/local.js (local dev listener).
PORT=5000
